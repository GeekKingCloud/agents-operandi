# Assistant Operations Context

This example repo controls an always-on assistant through prompts, cron routines, and channel setup scripts. Treat prompt and markdown changes as behavior changes. Static/offline evals are preferred for generic regression checks; live smoke tests must be opt-in and use test recipients or disposable profiles.
